<!DOCTYPE html>
<html>
<head>
    <title>Edit Recipe</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f2f2f2;
            margin: 0;
            padding: 0;
        }

        h1 {
            text-align: center;
            padding: 20px;
            background-color: steelblue;
            color: white;
        }

        form {
            width: 60%;
            margin: 0 auto;
            background-color: white;
            padding: 20px;
            border-radius: 5px;
            box-shadow: 0px 0px 5px rgba(0, 0, 0, 0.3);
        }

        label {
            font-weight: bold;
        }

        input[type="text"],
        textarea,
        select {
            width: 100%;
            padding: 10px;
            border: 1px solid #ccc;
            border-radius: 4px;
            margin-top: 6px;
            margin-bottom: 12px;
            resize: vertical;
        }

        input[type="submit"] {
            background-color: #4CAF50;
            color: white;
            padding: 10px 20px;
            border: none;
            border-radius: 4px;
            cursor: pointer;
        }

        input[type="submit"]:hover {
            background-color: #45a049;
        }

        textarea {
            height: 150px;
        }

        /* Additional styling for the photo display */
        #currentPhoto {
            max-width: 100%;
            height: auto;
        }
    </style>
</head>
<body>
    <h1>Edit Recipe - <%= recipe.title %></h1>
    <form class="recipe-form" action="/updateRecipe/<%= recipe.id %>" method="post" enctype="multipart/form-data">
        <label for="title">Recipe Title:</label>
        <input type="text" id="title" name="title" value="<%= recipe.title %>" required>
        <label for="makes">How many servings does the recipe make?:</label>
<input type="text" id="makes" name="makes" value="<%= recipe.makes %>" required>

        <label for="ingredients">Ingredients (separated by commas):</label>
        <input type="text" id="ingredients" name="ingredients" value="<%= recipe.ingredients %>" required>

        <label for="instructions">Instructions:</label>
        <textarea id="instructions" name="instructions" rows="4" required><%= recipe.instructions %></textarea>

        <label for="familySecrets">Family Secrets:</label>
        <textarea id="familySecrets" name="familySecrets" rows="4"><%= recipe.family_secrets %></textarea>

        <label for="type">Recipe Type:</label>
<select id="type" name="type">
    <option value="Appetizers" <%= recipe.type === 'Appetizers' ? 'selected' : '' %>>Appetizers</option>
    <option value="Breads" <%= recipe.type === 'Breads' ? 'selected' : '' %>>Breads</option>
    <option value="Soups" <%= recipe.type === 'Soups' ? 'selected' : '' %>>Soups</option>
    <option value="Pasta & Sauces" <%= recipe.type === 'Pasta & Sauces' ? 'selected' : '' %>>Pasta & Sauces</option>
    <option value="Entrées" <%= recipe.type === 'Entrées' ? 'selected' : '' %>>Entrées</option>
    <option value="Veggies" <%= recipe.type === 'Veggies' ? 'selected' : '' %>>Veggies</option>
    <option value="Cakes" <%= recipe.type === 'Cakes' ? 'selected' : '' %>>Cakes</option>
    <option value="Pies" <%= recipe.type === 'Pies' ? 'selected' : '' %>>Pies</option>
    <option value="Cookies" <%= recipe.type === 'Cookies' ? 'selected' : '' %>>Cookies</option>
</select>
<!-- Display the current photo -->
<label for="photo">Current Photo:</label>
<img id="currentPhoto" src="<%= recipe.image_url %>" alt="Recipe Photo">


<!-- Allow uploading a new photo -->
<label for="newPhoto">New Photo:</label>
<input type="file" id="newPhoto" name="newPhoto"> <!-- Use "newPhoto" as the name attribute -->

<input class="submit-button" type="submit" value="Update Recipe">
</form>

<a class="back-link" href="/myRecipes">Back to My Recipes</a>
</body>
</html>
